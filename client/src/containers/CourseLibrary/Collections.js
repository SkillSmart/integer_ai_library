import React from 'react';
import {connect} from 'react-redux';

const CourseCollections = (props) => {
    return (
        <div>
            <h2>Course Collections</h2>
        </div>
    )
}

export default connect()(CourseCollections);