import React, { Component } from 'react'
import {connect} from 'react-redux';

class ProjectUpdatePage extends Component {
  render() {
    return (
      <div>
        
      </div>
    )
  }
}

const mapStateToProps = (state, props) => ({

});

export default connect()(ProjectUpdatePage);